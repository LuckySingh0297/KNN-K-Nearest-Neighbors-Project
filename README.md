# KNN-K-Nearest-Neighbors-Project
Built a K-Nearest Neighbors (KNN) classification model to predict glass types using chemical composition data. Performed data preprocessing, EDA, feature scaling, cross-validation, and hyperparameter tuning to find the optimal K value. Evaluated model performance using accuracy, precision, recall, and confusion matrix.
